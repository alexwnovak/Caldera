using Caldera.Core;

namespace Caldera.EntryPoint
{
   internal static class Program
   {
      private static void Main( string[] arguments )
      {
         new MudApplication().Run( arguments );
      }
   }
}
